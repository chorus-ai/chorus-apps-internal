import FormBuilder from '../../../common/Form/FormBuilder';

export default function Form({ ____eIdx, form, values, setValues }) {

  return (
    <>
      <FormBuilder form={form} values={values} setValues={setValues} />
    </>
  )
}
